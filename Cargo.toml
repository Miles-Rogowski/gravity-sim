[package]
name = "gravity-sim"
version = "0.1.0"
edition = "2024"

[dependencies]
bevy = { version = "*", features = ["dynamic_linking"] }
image = "0.25.10"
noisy_bevy = "0.13.0"
rand = "0.10.0"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
